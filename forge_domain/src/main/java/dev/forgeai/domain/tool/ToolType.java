package dev.forgeai.domain.tool;

public enum ToolType {
    FILESYSTEM,
    GIT,
    GITHUB,
    TERMINAL,
    DOCKER,
    MAVEN,
    GRADLE,
    DATABASE,
    HTTP,
    BROWSER,
    AZURE,
    CUSTOM
}
